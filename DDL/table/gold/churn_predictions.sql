-- Databricks notebook source
CREATE TABLE ecommerce.gold.churn_predictions (
  customer_unique_id STRING NOT NULL,
  customer_state STRING,
  customer_city STRING,
  churn_probability DOUBLE,
  churn_label_predicted INT,
  risk_tier STRING,
  model_name STRING,
  model_version STRING,
  best_threshold DOUBLE,
  prediction_date STRING,
  job_id STRING,
  partition STRING,
  high_cutoff DOUBLE)
USING delta
COMMENT 'Daily churn predictions for all customers — generated by batch inference pipeline'
COLLATION 'UTF8_BINARY'
TBLPROPERTIES (
  'delta.autoOptimize.autoCompact' = 'true',
  'delta.autoOptimize.optimizeWrite' = 'true',
  'delta.enableDeletionVectors' = 'true',
  'delta.enableRowTracking' = 'true',
  'delta.feature.appendOnly' = 'supported',
  'delta.feature.deletionVectors' = 'supported',
  'delta.feature.domainMetadata' = 'supported',
  'delta.feature.invariants' = 'supported',
  'delta.feature.rowTracking' = 'supported',
  'delta.minReaderVersion' = '3',
  'delta.minWriterVersion' = '7',
  'delta.parquet.compression.codec' = 'zstd')
